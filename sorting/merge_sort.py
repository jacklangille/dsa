"""
Explanation:
    - Divide the list into two halves until each sublist has one element.
    - Merge sublists by comparing elements and sorting as they combine.

Efficiency:
    - Time: O(nlogn)
    - Space: O(n)

Good for large datasets where stability is needed, good for linked lists 
"""

def merge_sort(arr: list) -> list:
    


    return
